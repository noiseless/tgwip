Index: Telegram/SourceFiles/data/data_sponsored_messages.cpp
--- Telegram/SourceFiles/data/data_sponsored_messages.cpp.orig
+++ Telegram/SourceFiles/data/data_sponsored_messages.cpp
@@ -93,7 +93,7 @@ bool SponsoredMessages::append(not_null<History*> hist
 }
 
 bool SponsoredMessages::canHaveFor(not_null<History*> history) const {
-	return history->isChannel();
+	return false;
 }
 
 void SponsoredMessages::request(not_null<History*> history) {
@@ -189,7 +189,7 @@ void SponsoredMessages::clearItems(not_null<History*> 
 
 const SponsoredMessages::Entry *SponsoredMessages::find(
 		const FullMsgId &fullId) const {
-	if (!fullId.channel) {
+	if (true) {
 		return nullptr;
 	}
 	const auto history = _session->data().history(
@@ -210,7 +210,7 @@ const SponsoredMessages::Entry *SponsoredMessages::fin
 
 void SponsoredMessages::view(const FullMsgId &fullId) {
 	const auto entryPtr = find(fullId);
-	if (!entryPtr) {
+	if (true) {
 		return;
 	}
 	const auto randomId = entryPtr->sponsored.randomId;
