$OpenBSD$

Disable use of unported libdispatch;  from upstream
https://github.com/telegramdesktop/tdesktop/issues/17437#issuecomment-1001160398

Index: cmake/external/dispatch/CMakeLists.txt
--- cmake/external/dispatch/CMakeLists.txt.orig
+++ cmake/external/dispatch/CMakeLists.txt
@@ -19,6 +19,7 @@ if (DESKTOP_APP_USE_PACKAGED)
 endif()
 
 add_library(external_dispatch_bundled STATIC IMPORTED)
+return()
 
 set(dispatch_loc ${third_party_loc}/dispatch)
 set(dispatch_prefix ${CMAKE_CURRENT_BINARY_DIR}/dispatch-prefix)
